# Environment Variables for NTX Dashboard
# Copy this file to .env.local and fill in the values

# Supabase Configuration (optional - only needed for crypto verification API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Note: The dashboard works without these variables.
# They are only required for the /api/crypto/verify endpoint.
